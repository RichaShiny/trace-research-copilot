"use client";

import { useState } from "react";
import { ArrowUp, Check, ChevronDown, CircleHelp, FileText, Link2, Search, ShieldCheck, Sparkles } from "lucide-react";

const starterQuestion = "How do reasoning-unit queries improve evidence retrieval for multi-hop questions?";
const evidence = [
  { title: "Dense Passage Retrieval for Open-Domain QA", source: "Karpukhin et al. · EMNLP 2020", claim: "Dense retrievers can retrieve semantically relevant passages without relying on lexical overlap.", strength: "Direct" },
  { title: "HotpotQA: A Dataset for Diverse, Explainable Multi-hop QA", source: "Yang et al. · EMNLP 2018", claim: "Multi-hop questions require connecting evidence distributed across multiple documents.", strength: "Direct" },
  { title: "Your evaluation notes", source: "Experiment 006 · local workspace", claim: "Reasoning-unit retrieval reached 0.917 supporting-fact recall in an oracle evaluation, versus 0.542 for the sentence baseline.", strength: "Project evidence" },
];

export default function Home() {
  const [question, setQuestion] = useState(starterQuestion);
  const [run, setRun] = useState(true);
  function investigate() { if (question.trim()) setRun(true); }

  return <main className="min-h-screen bg-[#09111f] text-slate-100"><div className="mx-auto max-w-[1440px] px-5 py-5 md:px-9 md:py-8">
    <header className="mb-8 flex items-center justify-between"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-cyan-300 text-[#07111f] shadow-[0_0_32px_rgba(103,232,249,.23)]"><Sparkles size={20} strokeWidth={2.5} /></div><span className="text-lg font-semibold tracking-tight">Trace</span><span className="hidden rounded-full border border-slate-700 px-2.5 py-1 text-xs text-slate-400 sm:block">Research workspace</span></div><button className="flex items-center gap-2 rounded-lg border border-slate-700 bg-slate-800/50 px-3 py-2 text-sm text-slate-300 hover:border-slate-500"><CircleHelp size={16} /> How it works</button></header>
    <section className="mb-7 grid gap-5 lg:grid-cols-[1.3fr_.7fr]"><div><p className="mb-2 text-xs font-semibold uppercase tracking-[.18em] text-cyan-300">Evidence-first research</p><h1 className="max-w-2xl text-3xl font-semibold tracking-tight text-white md:text-4xl">Turn complex questions into answers you can verify.</h1></div><p className="self-end text-sm leading-6 text-slate-400">Trace breaks a question into evidence needs, then keeps every conclusion connected to its source.</p></section>
    <section className="rounded-2xl border border-slate-700/80 bg-[#101b2d] p-4 shadow-2xl shadow-black/15 md:p-5"><label htmlFor="question" className="mb-2 block text-sm font-medium text-slate-200">What are you trying to understand?</label><div className="flex flex-col gap-3 md:flex-row"><textarea id="question" value={question} onChange={(e) => { setQuestion(e.target.value); setRun(false); }} rows={2} className="min-h-[82px] flex-1 resize-none rounded-xl border border-slate-700 bg-[#07111f] px-4 py-3 text-[15px] leading-6 text-white outline-none placeholder:text-slate-600 focus:border-cyan-300" /><button onClick={investigate} className="flex items-center justify-center gap-2 rounded-xl bg-cyan-300 px-5 py-3 font-semibold text-[#07111f] transition hover:bg-cyan-200 md:self-stretch">Investigate <ArrowUp size={17} /></button></div><div className="mt-3 flex flex-wrap gap-2 text-xs text-slate-400"><span>Try:</span>{["Compare retrieval methods", "Find support for a claim"].map((item) => <button key={item} onClick={() => { setQuestion(item); setRun(false); }} className="rounded-full border border-slate-700 px-2.5 py-1 hover:border-cyan-300 hover:text-cyan-200">{item}</button>)}</div></section>
    {run && <><section className="mt-7 grid gap-5 xl:grid-cols-[.82fr_1.18fr]"><aside className="rounded-2xl border border-slate-800 bg-[#0d1728] p-5"><div className="mb-5 flex items-center justify-between"><h2 className="font-semibold">Evidence plan</h2><span className="text-xs text-slate-500">3 retrieval paths</span></div><ol className="space-y-5 border-l border-slate-700 pl-5"><Step number="01" title="Define the retrieval mechanism" body="What changes when a complex question is decomposed into smaller evidence-seeking queries?" active /><Step number="02" title="Establish the multi-hop baseline" body="What makes multi-hop evidence retrieval difficult for a single, original query?" /><Step number="03" title="Check measured results" body="Which experiment directly compares evidence recall for both approaches?" /></ol><button className="mt-6 flex items-center gap-2 text-sm text-cyan-300 hover:text-cyan-200"><ChevronDown size={16} /> Edit evidence plan</button></aside><div className="rounded-2xl border border-slate-800 bg-[#0d1728] p-5 md:p-6"><div className="mb-5 flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-cyan-300">Draft answer</p><h2 className="mt-1 text-xl font-semibold text-white">Why reasoning-unit queries help</h2></div><span className="flex items-center gap-1.5 rounded-full bg-emerald-400/10 px-3 py-1.5 text-xs font-medium text-emerald-300"><ShieldCheck size={14} /> 3 claims traced</span></div><p className="text-[15px] leading-7 text-slate-300">Reasoning-unit queries can improve multi-hop retrieval because they replace one overloaded question with smaller queries that correspond to its intermediate information needs. A dense retriever can then search for each missing fact independently, rather than trying to match every relation at once. In your oracle experiment, that decomposition created substantial headroom: supporting-fact recall rose from <mark>0.542</mark> with sentence retrieval to <mark>0.917</mark> with ideal reasoning units.</p><div className="mt-5 rounded-xl border border-cyan-300/15 bg-cyan-300/[.06] p-4"><p className="flex gap-2 text-sm font-medium text-cyan-100"><Sparkles className="mt-0.5 shrink-0 text-cyan-300" size={16} /> What this does—and does not—show</p><p className="mt-2 text-sm leading-6 text-slate-400">It shows better intermediate representations could unlock retrieval quality. It does not yet prove that an automatic generator can reliably create those representations.</p></div></div></section><section className="mt-5 rounded-2xl border border-slate-800 bg-[#0d1728] p-5 md:p-6"><div className="mb-5 flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-cyan-300">Claim ledger</p><h2 className="mt-1 text-xl font-semibold text-white">The evidence behind the answer</h2></div><button className="hidden items-center gap-2 text-sm text-slate-400 hover:text-white sm:flex"><FileText size={16} /> Export brief</button></div><div className="grid gap-3 md:grid-cols-3">{evidence.map((item) => <article key={item.title} className="rounded-xl border border-slate-700/70 bg-[#101b2d] p-4"><div className="mb-3 flex items-start justify-between gap-2"><span className="rounded-full bg-cyan-300/10 px-2 py-1 text-[11px] font-medium text-cyan-200">{item.strength}</span><Link2 size={15} className="text-slate-500" /></div><h3 className="text-sm font-semibold leading-5 text-slate-100">{item.title}</h3><p className="mt-1 text-xs text-slate-500">{item.source}</p><p className="mt-3 text-sm leading-6 text-slate-400">{item.claim}</p><div className="mt-4 flex items-center gap-1.5 text-xs text-emerald-300"><Check size={14} /> Supports a visible claim</div></article>)}</div></section></>}
  </div></main>;
}
function Step({ number, title, body, active = false }: { number: string; title: string; body: string; active?: boolean }) { return <li className="relative"><span className={`absolute -left-[35px] top-0 grid h-5 w-5 place-items-center rounded-full border text-[9px] font-bold ${active ? "border-cyan-300 bg-cyan-300 text-[#07111f]" : "border-slate-600 bg-[#0d1728] text-slate-500"}`}>{active ? <Search size={11} /> : number.slice(-1)}</span><h3 className="text-sm font-semibold text-slate-200">{title}</h3><p className="mt-1 text-sm leading-6 text-slate-400">{body}</p></li>; }
